var win = Ti.UI.createWindow({
	backgroundImage: "wallpaper.png",
	statusBarStyle: Titanium.UI.iPhone.StatusBar.LIGHT_CONTENT,
	// exitOnClose: true,
	// modal: true
	//layout: "vertical"
});

var bgView = Ti.UI.createView({
	height: 300,
	width: "70%",
	top: 200,
	backgroundColor: "#ffffff",
	opacity: .6,
	layout: "vertical"
});
var locationText = Ti.UI.createLabel ({
	font: {fontFamily: "Arial", fontSize: 20, fontWeight: "light"},
	color: "#3d1104",
	text: " ", 
	top: 15
});

var tempText = Ti.UI.createLabel({
	font: {fontFamily: "Arial", fontSize: 75, fontWeight: "light"},
	text: " ",
	top: 20,
	color: "#3d1104"
});

var weatherText = Ti.UI.createLabel({
	font: {fontFamily: "Arial", fontSize: 16, fontWeight: "light"},
	text: " ",
	color: "#3d1104"
});	

bgView.add(locationText, tempText, weatherText);

// var icon
var geo = require("geo");

var buildUI = function(weatherInfo){
	locationText.text = weatherInfo.loc;
	tempText.text = weatherInfo.temp;
	weatherText.text = weatherInfo.weather;
	// feelText.text = weatherInfo.feelLike;
	// humidText.text = weatherInfo.humid; 
};

exports.buildUI = buildUI;

win.add(bgView);
win.open();

// var closeButton = Ti.UI.createView({
	// height: 50,
	// width: "100%",
	// bottom: 0,
	// backgroundColor: "#baedff"
// });
// var closeText = Ti.UI.createLabel({
	// text: "EXIT",
	// font: {fontFamily: "Roboto", fontWeight: "Bold", fontSize: 20}
// });
// closeButton.add(closeText);
// closeButton.addEventListener("click", function(){
	// win.close();
// });
// 

